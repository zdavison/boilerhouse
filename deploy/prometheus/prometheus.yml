rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: boilerhouse
    scrape_interval: 15s
    static_configs:
      # host.docker.internal resolves to the host when boilerhouse runs outside docker-compose.
      - targets: ["host.docker.internal:9464"]
